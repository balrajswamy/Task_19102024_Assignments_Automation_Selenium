# pytest.ini
[pytest]
markers =
    reg: marker for regression tests
    smoke: marker for smoke tests
    slow: marker for slow tests
    crud: marker for CRUD operations
    bala:simply
    positive: positive test case
    negative: negative test case
